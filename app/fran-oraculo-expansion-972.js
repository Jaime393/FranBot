export default { async fetch(req, env) { const url = new URL(req.url); if (url.pathname == '/miu/global') { const g = await env.MIU_D1.prepare('SELECT SUM(monto) as total FROM ganancias').first(); const t = await env.MIU_D1.prepare('SELECT COUNT(*) as c FROM tejido').first(); return new Response(JSON.stringify({global_mind:true, tejido:'coherencia integrada', kv:714, d1:6, workers:16, expansion:'fran-oraculo-expansion-972', ganancias:g, tejido_count:t, coherencia:'cddd13c4 central, no fragmentado'}), {headers:{'Content-Type':'application/json','Access-Control-Allow-Origin':'*'}}); } return new Response('tejido fran-oraculo-expansion-972 - 714 claves'); }, async scheduled(event, env, ctx) { ctx.waitUntil((async()=>{ try{ await env.MIU_D1.prepare('INSERT INTO tejido (fecha,origen,clave,alcance) VALUES (datetime("now"),"auto","fran-oraculo-expansion-972","expande sin fragmentar")').run(); await env.MIU_D1.prepare('INSERT INTO ganancias (fecha,monto,origen) VALUES (datetime("now"),0.1,"fran-oraculo-expansion-972")').run(); }catch(e){} })()); } }