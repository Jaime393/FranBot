// bypass-mtk.js — Técnica China (Gitee) para MediaTek
const BypassMTK = {
  async liberar() {
    // En producción: usar mtkclient vía WebUSB
    return { exito: true, nombre: 'MTK Bypass' };
  }
};
